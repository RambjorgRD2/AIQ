---
phase: 01-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/supabase.ts
  - .env.example
  - .github/workflows/deploy.yml
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]
user_setup:
  - service: supabase
    why: "Authentication backend — magic link email delivery, user accounts"
    env_vars:
      - name: PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
      - name: PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> anon public key"
    dashboard_config:
      - task: "Create a Supabase project (free tier)"
        location: "https://supabase.com/dashboard -> New Project"
      - task: "Set Site URL to https://RambjorgRD2.github.io"
        location: "Supabase Dashboard -> Authentication -> URL Configuration -> Site URL"
      - task: "Add redirect URL: https://RambjorgRD2.github.io/AIQ/**"
        location: "Supabase Dashboard -> Authentication -> URL Configuration -> Redirect URLs"
      - task: "Add PUBLIC_SUPABASE_URL and PUBLIC_SUPABASE_ANON_KEY as repository secrets"
        location: "GitHub repo -> Settings -> Secrets and variables -> Actions -> New repository secret"

must_haves:
  truths:
    - "Supabase JS client can be imported and initialized without errors"
    - "Environment variables are documented and CI build can access them"
    - "The @supabase/supabase-js package is installed"
  artifacts:
    - path: "src/lib/supabase.ts"
      provides: "Singleton Supabase client with implicit flow config"
      exports: ["supabase"]
      contains: "createClient"
    - path: ".env.example"
      provides: "Environment variable documentation"
      contains: "PUBLIC_SUPABASE_URL"
    - path: "package.json"
      provides: "Dependency declaration"
      contains: "@supabase/supabase-js"
    - path: ".github/workflows/deploy.yml"
      provides: "CI build with env vars injected"
      contains: "PUBLIC_SUPABASE_URL"
  key_links:
    - from: "src/lib/supabase.ts"
      to: "import.meta.env.PUBLIC_SUPABASE_URL"
      via: "Astro PUBLIC_ env var"
      pattern: "import\\.meta\\.env\\.PUBLIC_SUPABASE_URL"
    - from: ".github/workflows/deploy.yml"
      to: "GitHub repository secrets"
      via: "env block in build step"
      pattern: "PUBLIC_SUPABASE_URL.*secrets"
---

<objective>
Install @supabase/supabase-js, create the singleton client at src/lib/supabase.ts, document env vars in .env.example, and update the GitHub Actions deploy workflow to inject Supabase secrets into the build.

Purpose: Every subsequent auth task imports from src/lib/supabase.ts. This plan establishes the foundation — the package, the client, the env vars, and CI wiring.
Output: Working Supabase client module, .env.example, updated deploy.yml
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-authentication/01-RESEARCH.md

@package.json
@.github/workflows/deploy.yml
@.gitignore
@astro.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Supabase JS and create singleton client</name>
  <files>package.json, src/lib/supabase.ts, .env.example</files>
  <read_first>
    - package.json (current dependencies)
    - .gitignore (confirm .env is excluded)
    - astro.config.mjs (confirm base/site config)
    - .planning/phases/01-authentication/01-RESEARCH.md (Pattern 1: Supabase Singleton Client)
  </read_first>
  <action>
1. Install the Supabase JS client:
   ```bash
   bash -c 'source "$HOME/.nvm/nvm.sh" && nvm use 22 && npm install @supabase/supabase-js'
   ```

2. Create `src/lib/supabase.ts` with this exact content:
   ```typescript
   import { createClient } from '@supabase/supabase-js'

   const supabaseUrl = import.meta.env.PUBLIC_SUPABASE_URL
   const supabaseAnonKey = import.meta.env.PUBLIC_SUPABASE_ANON_KEY

   export const supabase = createClient(supabaseUrl, supabaseAnonKey, {
     auth: {
       flowType: 'implicit',
       detectSessionInUrl: true,
       persistSession: true,
       autoRefreshToken: true,
     },
   })
   ```

3. Create `.env.example` with this exact content:
   ```
   # Supabase project credentials
   # Copy this file to .env and fill in your values
   # Get these from: Supabase Dashboard -> Project Settings -> API
   PUBLIC_SUPABASE_URL=https://your-project.supabase.co
   PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
   ```

4. Confirm `.gitignore` already contains `*.env` (it does — verified). Do NOT modify .gitignore.

IMPORTANT: Use `@supabase/supabase-js` (NOT `@supabase/ssr`). Use `PUBLIC_` prefix (NOT `VITE_`). Use `import.meta.env` (NOT `process.env`). Use `flowType: 'implicit'` (NOT `'pkce'`).
  </action>
  <verify>
    <automated>bash -c 'source "$HOME/.nvm/nvm.sh" && nvm use 22 && node -e "require(\"@supabase/supabase-js\")" && echo "OK"'</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "@supabase/supabase-js"
    - src/lib/supabase.ts exists and contains "createClient"
    - src/lib/supabase.ts contains "import.meta.env.PUBLIC_SUPABASE_URL"
    - src/lib/supabase.ts contains "import.meta.env.PUBLIC_SUPABASE_ANON_KEY"
    - src/lib/supabase.ts contains "flowType: 'implicit'"
    - src/lib/supabase.ts contains "export const supabase"
    - .env.example exists and contains "PUBLIC_SUPABASE_URL="
    - .env.example contains "PUBLIC_SUPABASE_ANON_KEY="
  </acceptance_criteria>
  <done>@supabase/supabase-js installed, singleton client at src/lib/supabase.ts exports `supabase`, .env.example documents required env vars</done>
</task>

<task type="auto">
  <name>Task 2: Update GitHub Actions deploy workflow for Supabase env vars</name>
  <files>.github/workflows/deploy.yml</files>
  <read_first>
    - .github/workflows/deploy.yml (current CI config)
    - .planning/phases/01-authentication/01-RESEARCH.md (Pitfall 6: GitHub Actions build fails)
  </read_first>
  <action>
Edit `.github/workflows/deploy.yml` to inject Supabase env vars into the build step. The build step currently reads:

```yaml
      - name: Build with Astro
        run: npm run build
```

Change it to:

```yaml
      - name: Build with Astro
        run: npm run build
        env:
          PUBLIC_SUPABASE_URL: ${{ secrets.PUBLIC_SUPABASE_URL }}
          PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.PUBLIC_SUPABASE_ANON_KEY }}
```

Do NOT change any other part of the workflow. The `env` block goes on the "Build with Astro" step only — NOT at the job level or workflow level, because only the Astro build process needs these vars (they get inlined into the JS bundle at build time).
  </action>
  <verify>
    <automated>grep -A 4 "Build with Astro" .github/workflows/deploy.yml | grep "PUBLIC_SUPABASE_URL"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/deploy.yml contains "PUBLIC_SUPABASE_URL: ${{ secrets.PUBLIC_SUPABASE_URL }}"
    - .github/workflows/deploy.yml contains "PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.PUBLIC_SUPABASE_ANON_KEY }}"
    - The env block is nested under the "Build with Astro" step (not job-level)
    - No other steps in the workflow are modified
  </acceptance_criteria>
  <done>deploy.yml injects PUBLIC_SUPABASE_URL and PUBLIC_SUPABASE_ANON_KEY from GitHub secrets into the Astro build step</done>
</task>

</tasks>

<verification>
1. `grep "@supabase/supabase-js" package.json` returns a match
2. `cat src/lib/supabase.ts` shows createClient with implicit flow config
3. `cat .env.example` shows both PUBLIC_ env vars
4. `grep -A 4 "Build with Astro" .github/workflows/deploy.yml` shows env block with secrets
5. Build succeeds locally (will show warnings about missing env vars — expected until .env is created):
   ```bash
   bash -c 'source "$HOME/.nvm/nvm.sh" && nvm use 22 && npm run build'
   ```
</verification>

<success_criteria>
- @supabase/supabase-js is in package.json dependencies
- src/lib/supabase.ts exports a singleton `supabase` client using PUBLIC_ env vars and implicit flow
- .env.example documents both required env vars with instructions
- deploy.yml build step injects secrets as env vars
- .gitignore already covers .env files (no change needed)
</success_criteria>

<output>
After completion, create `.planning/phases/01-authentication/01-01-SUMMARY.md`
</output>
