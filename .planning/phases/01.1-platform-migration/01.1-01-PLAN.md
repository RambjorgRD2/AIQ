---
phase: 01.1-platform-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - astro.config.mjs
  - src/pages/signin.astro
  - src/pages/auth/callback.astro
  - src/layouts/Layout.astro
  - src/pages/results.astro
autonomous: true
requirements:
  - MIG-01
  - MIG-02
  - MIG-05

must_haves:
  truths:
    - "npm run build completes without errors"
    - "No /AIQ/ prefix appears anywhere in the built dist/ output"
    - "Sign-out redirects to / not /AIQ/"
    - "Auth callback fallback redirects to / not /AIQ/"
    - "Share URL on results page points to www.theaiq.org/assessment"
  artifacts:
    - path: "astro.config.mjs"
      provides: "Astro config without base, with correct site URL"
      contains: "https://www.theaiq.org"
    - path: "src/pages/signin.astro"
      provides: "Magic link form with updated emailRedirectTo and fallback redirect"
      contains: "https://www.theaiq.org/auth/callback"
    - path: "src/pages/auth/callback.astro"
      provides: "Auth callback with updated fallback redirect"
      contains: "|| '/'"
    - path: "src/layouts/Layout.astro"
      provides: "Layout with updated sign-out redirect"
      contains: "window.location.href = '/'"
    - path: "src/pages/results.astro"
      provides: "Results page with updated share URL"
      contains: "https://www.theaiq.org/assessment"
  key_links:
    - from: "astro.config.mjs"
      to: "import.meta.env.BASE_URL"
      via: "Astro build system"
      pattern: "site.*theaiq\\.org"
    - from: "src/pages/signin.astro"
      to: "https://www.theaiq.org/auth/callback"
      via: "emailRedirectTo in signInWithOtp"
      pattern: "emailRedirectTo.*theaiq"
---

<objective>
Remove the /AIQ base path from Astro config and fix all 6 hardcoded /AIQ/ strings in 5 source files.

Purpose: With base removed, BASE_URL becomes '/' and all ${BASE}/path patterns automatically resolve to /path. The 6 hardcoded strings bypass this mechanism and must be fixed explicitly. This is a prerequisite for Plan 02 (Vercel setup) because the build must produce correct output URLs before deployment.

Output: Clean build with no /AIQ/ prefix in any output URLs; sign-out, callback, and share URLs all point to the correct domain paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-platform-migration/01.1-RESEARCH.md

<interfaces>
<!-- Key patterns and exact strings the executor needs. Extracted from source files. -->
<!-- Executor must fix these exact occurrences — no codebase exploration needed. -->

From astro.config.mjs (current):
```javascript
export default defineConfig({
  site: 'https://RambjorgRD2.github.io/AIQ',
  base: '/AIQ',
  integrations: [tailwind()],
});
```

From src/pages/signin.astro line 115:
```javascript
sessionStorage.setItem('auth_redirect', document.referrer || '/AIQ/')
```

From src/pages/signin.astro line 120:
```javascript
emailRedirectTo: 'https://RambjorgRD2.github.io/AIQ/auth/callback',
```

From src/pages/results.astro line 726:
```javascript
const shareUrl = 'https://rambjorgrd2.github.io/AIQ/assessment';
```

From src/pages/auth/callback.astro lines 64 and 73:
```javascript
const redirectTo = sessionStorage.getItem('auth_redirect') || '/AIQ/'
```

From src/layouts/Layout.astro line 99:
```javascript
window.location.href = '/AIQ/'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove base from astro.config.mjs and update site URL</name>
  <files>astro.config.mjs</files>
  <action>
Replace the entire content of astro.config.mjs with:

```javascript
import { defineConfig } from 'astro/config';
import tailwind from '@astrojs/tailwind';

export default defineConfig({
  site: 'https://www.theaiq.org',
  integrations: [tailwind()],
});
```

Critical: Do NOT set `base: '/'` — omit the base key entirely. Setting base to '/' is a no-op that creates confusion. With base absent, import.meta.env.BASE_URL resolves to '/' at build time, making `${BASE}` an empty string after the .replace(/\/$/, '') pattern already present in every page. All existing ${BASE}/path patterns will continue to produce /path correctly — no per-file changes needed for those.

After editing, run the build to confirm no Astro config errors:
bash -c 'source "$HOME/.nvm/nvm.sh" && nvm use 22 && npm run build'

The build must complete without errors. Ignore any warnings about missing PUBLIC_SUPABASE_URL — those are env-var-only warnings, not blockers for config validation.
  </action>
  <verify>
    <automated>bash -c 'source "$HOME/.nvm/nvm.sh" && nvm use 22 && npm run build 2>&1 | tail -5'</automated>
  </verify>
  <done>astro.config.mjs contains site: 'https://www.theaiq.org' and no base key. Build exits 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix all 6 hardcoded /AIQ/ strings across 5 source files</name>
  <files>
    src/pages/signin.astro,
    src/pages/auth/callback.astro,
    src/layouts/Layout.astro,
    src/pages/results.astro
  </files>
  <action>
Make these exact targeted replacements (6 total occurrences across 4 files):

**src/pages/signin.astro — 2 changes:**

1. Line ~115 — fallback redirect after sign-in:
   Find:    `sessionStorage.setItem('auth_redirect', document.referrer || '/AIQ/')`
   Replace: `sessionStorage.setItem('auth_redirect', document.referrer || '/')`

2. Line ~120 — emailRedirectTo for magic link:
   Find:    `emailRedirectTo: 'https://RambjorgRD2.github.io/AIQ/auth/callback',`
   Replace: `emailRedirectTo: 'https://www.theaiq.org/auth/callback',`

**src/pages/auth/callback.astro — 2 changes (same pattern, two lines):**

3. Line ~64 — onAuthStateChange redirect fallback:
   Find:    `const redirectTo = sessionStorage.getItem('auth_redirect') || '/AIQ/'`
   Replace: `const redirectTo = sessionStorage.getItem('auth_redirect') || '/'`

4. Line ~73 — getSession fallback:
   Find:    `const redirectTo = sessionStorage.getItem('auth_redirect') || '/AIQ/'`
   Replace: `const redirectTo = sessionStorage.getItem('auth_redirect') || '/'`

**src/layouts/Layout.astro — 1 change:**

5. Line ~99 — sign-out redirect after countdown:
   Find:    `window.location.href = '/AIQ/'`
   Replace: `window.location.href = '/'`

**src/pages/results.astro — 1 change:**

6. Line ~726 — share URL for social sharing buttons:
   Find:    `const shareUrl = 'https://rambjorgrd2.github.io/AIQ/assessment';`
   Replace: `const shareUrl = 'https://www.theaiq.org/assessment';`

After all replacements, verify zero remaining occurrences:
grep -r '/AIQ' src/ --include="*.astro" --include="*.ts" --include="*.js"

This must return empty output. If any match remains, fix it before proceeding.

Then run a final build to confirm the full build still succeeds:
bash -c 'source "$HOME/.nvm/nvm.sh" && nvm use 22 && npm run build'
  </action>
  <verify>
    <automated>bash -c 'source "$HOME/.nvm/nvm.sh" && nvm use 22 && grep -r "/AIQ" src/ --include="*.astro" --include="*.ts" --include="*.js" && echo "FAIL: /AIQ strings remain" || echo "PASS: no /AIQ strings in src/"'</automated>
  </verify>
  <done>grep -r '/AIQ' src/ returns empty. npm run build exits 0. dist/ output contains no /AIQ prefix in any href, src, or action attribute.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `grep -r '/AIQ' src/ --include="*.astro" --include="*.ts" --include="*.js"` returns empty
2. `grep -r '/AIQ' dist/` returns empty (no /AIQ in built output)
3. `npm run build` exits 0
4. astro.config.mjs contains `site: 'https://www.theaiq.org'` and no `base` key
</verification>

<success_criteria>
- astro.config.mjs: site = 'https://www.theaiq.org', no base key
- All 6 hardcoded /AIQ/ strings replaced (confirmed by grep returning empty)
- npm run build succeeds (exit 0)
- dist/ output has no /AIQ prefix in any URL
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-platform-migration/01.1-01-SUMMARY.md`
</output>
