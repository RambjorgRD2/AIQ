---
phase: 01.1-platform-migration
plan: 01
subsystem: build-config
tags: [base-path, astro-config, domain-migration, url-cleanup]
dependency_graph:
  requires: []
  provides: [clean-build-no-base-path, correct-domain-urls]
  affects: [astro.config.mjs, signin.astro, auth/callback.astro, Layout.astro, results.astro]
tech_stack:
  added: []
  patterns: [astro-no-base-config, absolute-domain-hardcode]
key_files:
  created: []
  modified:
    - astro.config.mjs
    - src/pages/signin.astro
    - src/pages/auth/callback.astro
    - src/layouts/Layout.astro
    - src/pages/results.astro
decisions:
  - "Omit base key entirely from astro.config.mjs rather than setting base: '/' — a no-op that creates confusion"
  - "Display URL in score card (tier-id-url span) updated to www.theaiq.org alongside functional URL fixes"
metrics:
  duration: 83s
  completed: "2026-03-29"
  tasks: 2
  files: 5
---

# Phase 01.1 Plan 01: Remove /AIQ Base Path and Update Domain URLs Summary

**One-liner:** Astro base path removed and all 7 hardcoded /AIQ/ or GitHub Pages strings replaced with www.theaiq.org equivalents, producing a clean build with no /AIQ prefix anywhere.

## What Was Built

Removed the `/AIQ` base path from `astro.config.mjs` and updated the site URL to `https://www.theaiq.org`. Fixed all hardcoded domain/path strings across 4 source files so they reference the new domain. The build now produces 71 pages with no `/AIQ` prefix in any output URL.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Remove base from astro.config.mjs, update site URL | 09d33e5 | astro.config.mjs |
| 2 | Fix all hardcoded /AIQ/ strings across 4 files | 9bde05d | signin.astro, callback.astro, Layout.astro, results.astro |

## Verification Results

- `grep -r '/AIQ' src/` returns empty — PASS
- `grep -r '/AIQ' dist/` returns empty — PASS
- `npm run build` exits 0, 71 pages built — PASS
- `astro.config.mjs` contains `site: 'https://www.theaiq.org'` with no `base` key — PASS

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed additional /AIQ display string in results.astro score card**
- **Found during:** Task 2 verification — grep returned one more occurrence after the 6 specified in the plan
- **Issue:** `<span class="tier-id-url">rambjorgrd2.github.io/AIQ</span>` at line 122 — display text shown on the shareable score card still referenced the old GitHub Pages URL
- **Fix:** Replaced with `www.theaiq.org`
- **Files modified:** src/pages/results.astro (same file, line 122)
- **Commit:** 9bde05d (included in Task 2 commit)

The plan documented 6 occurrences across 4 files; there were actually 7 across the same 4 files. The 7th was a visual display string, not a functional redirect, but it would have shown the wrong domain on every shared score card.

## Known Stubs

None. All URL changes are complete and functional.

## Self-Check: PASSED

- astro.config.mjs found and contains correct site URL with no base key
- Task 1 commit 09d33e5 found in git log
- Task 2 commit 9bde05d found in git log
- grep /AIQ in src/ returns empty
- grep /AIQ in dist/ returns empty
- npm run build exits 0
