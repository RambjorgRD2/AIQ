---
phase: 01.1-platform-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 01.1-01
files_modified:
  - .github/workflows/deploy.yml
  - .gitignore
autonomous: false
requirements:
  - MIG-01
  - MIG-03
  - MIG-04
  - MIG-06

user_setup:
  - service: vercel
    why: "Vercel project must exist and be linked before CI can deploy"
    env_vars:
      - name: VERCEL_TOKEN
        source: "vercel.com → Account Settings → Tokens → Create Token"
      - name: VERCEL_ORG_ID
        source: "Run 'vercel link' in project root → .vercel/project.json → orgId field"
      - name: VERCEL_PROJECT_ID
        source: "Run 'vercel link' in project root → .vercel/project.json → projectId field"
      - name: PUBLIC_SUPABASE_URL
        source: "Already in GitHub Secrets — also add to Vercel Dashboard → Project Settings → Environment Variables → Production"
      - name: PUBLIC_SUPABASE_ANON_KEY
        source: "Already in GitHub Secrets — also add to Vercel Dashboard → Project Settings → Environment Variables → Production"
    dashboard_config:
      - task: "Link GitHub repo to Vercel project"
        location: "vercel.com → New Project → Import Git Repository → select this repo"
      - task: "Add PUBLIC_SUPABASE_URL and PUBLIC_SUPABASE_ANON_KEY as Production environment variables"
        location: "Vercel Dashboard → Project → Settings → Environment Variables"
      - task: "Add VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID as repository secrets"
        location: "GitHub repo → Settings → Secrets and variables → Actions → New repository secret"
      - task: "Update Supabase Auth Site URL and Redirect URLs"
        location: "Supabase Dashboard → Authentication → URL Configuration → Site URL = https://www.theaiq.org, add https://www.theaiq.org/auth/callback to Redirect URLs"
      - task: "Disable GitHub Pages after Vercel is confirmed working"
        location: "GitHub repo → Settings → Pages → Source → set to None"

must_haves:
  truths:
    - "Pushing to main triggers Vercel deployment (not GitHub Pages)"
    - "Vercel deployment succeeds with Supabase env vars baked into the JS bundle"
    - "Magic link sends to www.theaiq.org/auth/callback (confirmed in Supabase dashboard)"
    - ".vercel/project.json is not committed to git"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "Vercel GitHub Actions deployment workflow"
      contains: "vercel deploy --prebuilt --prod"
    - path: ".gitignore"
      provides: "Prevents .vercel/ directory from being committed"
      contains: ".vercel"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "Vercel production deployment"
      via: "vercel deploy --prebuilt --prod with VERCEL_TOKEN"
      pattern: "vercel deploy.*prod"
    - from: "vercel pull"
      to: "PUBLIC_SUPABASE_URL, PUBLIC_SUPABASE_ANON_KEY"
      via: "Vercel dashboard environment variables"
      pattern: "vercel pull.*production"
---

<objective>
Replace the GitHub Pages workflow with Vercel deployment, add .vercel/ to .gitignore, and coordinate the human setup steps (Vercel project linking, GitHub Secrets, Supabase dashboard URL update) needed for CI to succeed.

Purpose: This plan wires together the CI side of the migration. The code changes from Plan 01 are already correct — this plan makes the deployment target switch. The Supabase redirect URL must also be updated in the dashboard to match the emailRedirectTo change made in Plan 01.

Output: .github/workflows/deploy.yml deploys to Vercel on push to main. Supabase auth redirect configured for www.theaiq.org. .vercel/ excluded from git.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.1-platform-migration/01.1-RESEARCH.md
@.planning/phases/01.1-platform-migration/01.1-01-SUMMARY.md

<interfaces>
<!-- Current GitHub Pages deploy.yml (to be replaced): -->
<!-- name: Deploy to GitHub Pages -->
<!-- push to main → npm ci → npm run build → actions/deploy-pages@v4 -->
<!-- Supabase vars injected in Build step via env: block -->
<!-- No vercel-related secrets exist yet -->

<!-- Target: Vercel deploy workflow using vercel CLI pull/build/deploy pattern -->
<!-- Required GitHub Secrets (new): VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID -->
<!-- Required Vercel dashboard env vars: PUBLIC_SUPABASE_URL, PUBLIC_SUPABASE_ANON_KEY -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add .vercel/ to .gitignore and replace deploy.yml with Vercel workflow</name>
  <files>.gitignore, .github/workflows/deploy.yml</files>
  <action>
**Step A — .gitignore:**
Add `.vercel/` as a new line to .gitignore. This prevents the `vercel link` output (.vercel/project.json containing orgId and projectId) from being committed. Read the current .gitignore first and append the entry without disturbing existing lines.

**Step B — Replace .github/workflows/deploy.yml:**
Overwrite the file completely with the following content. This replaces the GitHub Pages workflow entirely (do not keep both):

```yaml
name: Vercel Production Deployment

env:
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  Deploy-Production:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      - name: Pull Vercel Environment Information
        run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}

      - name: Build Project Artifacts
        run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}
        env:
          PUBLIC_SUPABASE_URL: ${{ secrets.PUBLIC_SUPABASE_URL }}
          PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.PUBLIC_SUPABASE_ANON_KEY }}

      - name: Deploy Project Artifacts to Vercel
        run: vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }}
```

Notes on this workflow:
- `vercel pull` downloads Vercel project configuration and any env vars set in the dashboard
- `vercel build --prod` runs the Astro build inside Vercel's build system (producing .vercel/output/)
- The `env:` block on the Build step is belt-and-suspenders — ensures Supabase vars are available even if not yet added to Vercel dashboard
- `vercel deploy --prebuilt --prod` uploads the pre-built output without rebuilding
- No GitHub Pages permissions block needed (pages: write, id-token: write removed)
  </action>
  <verify>
    <automated>bash -c 'grep -q "vercel deploy --prebuilt --prod" "/Users/jdm/Library/CloudStorage/Dropbox/Personlig [Stash]/Business/A.I. Adventures/Claude/AIQ/.github/workflows/deploy.yml" && echo "PASS: Vercel workflow in place" || echo "FAIL: deploy.yml not updated"'</automated>
  </verify>
  <done>.gitignore contains .vercel/ entry. deploy.yml contains Vercel workflow (no GitHub Pages references). File validates as valid YAML.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Human setup — Vercel project, GitHub Secrets, Supabase dashboard</name>
  <what-to-do>
Complete these setup steps before the CI workflow can run successfully. They require dashboard access that Claude cannot automate.

**Step 1 — Create Vercel project and link the repo:**
1. Go to vercel.com → New Project
2. Import Git Repository → select the AIQ GitHub repo
3. Framework Preset: Astro (Vercel detects this automatically)
4. Root Directory: leave as default (project root)
5. Click Deploy — this creates the Vercel project (first deploy will fail on Supabase vars until Step 3, that's fine)

**Step 2 — Get VERCEL_ORG_ID and VERCEL_PROJECT_ID:**

Option A (CLI — recommended):
```bash
# Install Vercel CLI if not already installed
npm install --global vercel@latest
# In the project root:
vercel link
# Select the project you just created
cat .vercel/project.json
# Note: orgId and projectId from the output
```

Option B (Dashboard):
- VERCEL_PROJECT_ID: Vercel Dashboard → Project → Settings → General → Project ID
- VERCEL_ORG_ID: vercel.com → Account Settings → General → Your ID (or Team ID if using a team)

**Step 3 — Create VERCEL_TOKEN:**
- Go to vercel.com → Account Settings → Tokens
- Create Token: name it "github-actions-aiq", scope: Full Account
- Copy the token immediately (only shown once)

**Step 4 — Add GitHub repository secrets** (GitHub repo → Settings → Secrets and variables → Actions):
- `VERCEL_TOKEN` → the token from Step 3
- `VERCEL_ORG_ID` → orgId from Step 2
- `VERCEL_PROJECT_ID` → projectId from Step 2
(PUBLIC_SUPABASE_URL and PUBLIC_SUPABASE_ANON_KEY already exist — no change needed)

**Step 5 — Add Supabase env vars to Vercel dashboard:**
- Vercel Dashboard → Project → Settings → Environment Variables
- Add `PUBLIC_SUPABASE_URL` → same value as in GitHub Secrets
- Add `PUBLIC_SUPABASE_ANON_KEY` → same value as in GitHub Secrets
- Environment: Production (and Preview if desired)

**Step 6 — Update Supabase Auth URL Configuration:**
- Supabase Dashboard → project → Authentication → URL Configuration
- Site URL: change to `https://www.theaiq.org`
- Redirect URLs: add `https://www.theaiq.org/auth/callback`
- Save changes
- (Optionally keep the old GitHub Pages callback URL in the list during transition)

**Step 7 — Trigger a test deployment:**
Push any trivial change to main (or use the GitHub Actions "Run workflow" button) and confirm the "Vercel Production Deployment" workflow succeeds in the Actions tab.

**Step 8 — Disable GitHub Pages (after Vercel is confirmed working):**
- GitHub repo → Settings → Pages → Source → None → Save
  </what-to-do>
  <resume-signal>Type "deployed" when the Vercel workflow has run successfully (green checkmark in GitHub Actions), or describe any failures encountered.</resume-signal>
</task>

</tasks>

<verification>
After both tasks complete:

1. `.github/workflows/deploy.yml` — contains "Vercel Production Deployment", no GitHub Pages references
2. `.gitignore` — contains `.vercel/` entry
3. GitHub Actions — "Vercel Production Deployment" workflow shows green on last run
4. Vercel dashboard — shows a successful production deployment of the AIQ project
5. The deployment is accessible at the Vercel-provided `*.vercel.app` URL (www.theaiq.org DNS may not yet be live)
6. Auth works: trigger a magic link from the *.vercel.app URL — email arrives with www.theaiq.org/auth/callback URL (confirms Supabase dashboard was updated)
</verification>

<success_criteria>
- Vercel deployment workflow runs successfully on push to main
- Supabase PUBLIC_SUPABASE_URL and PUBLIC_SUPABASE_ANON_KEY baked into build (auth module initializes without errors)
- Supabase Auth Site URL and Redirect URLs updated to www.theaiq.org
- .vercel/ excluded from git
- GitHub Pages workflow removed (not just disabled)
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-platform-migration/01.1-02-SUMMARY.md`
</output>
