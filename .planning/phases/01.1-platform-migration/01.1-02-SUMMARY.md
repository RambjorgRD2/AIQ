---
phase: 01.1-platform-migration
plan: 02
subsystem: infra
tags: [vercel, github-actions, ci-cd, deploy, gitignore]

requires:
  - phase: 01.1-platform-migration
    provides: [clean-build-no-base-path, correct-domain-urls]

provides:
  - Vercel GitHub Actions deployment workflow replacing GitHub Pages
  - .vercel/ excluded from git

affects: [vercel-setup, supabase-auth-config, dns-config]

tech-stack:
  added: [vercel-cli]
  patterns: [vercel-pull-build-deploy, belt-and-suspenders-env-vars]

key-files:
  created: []
  modified:
    - .github/workflows/deploy.yml
    - .gitignore

key-decisions:
  - "Use vercel pull/build/deploy pattern (not vercel deploy from source) — enables prebuilt upload and proper env var injection via Vercel dashboard"
  - "Belt-and-suspenders env block on vercel build step — Supabase vars available even if not yet added to Vercel dashboard"
  - "VERCEL_ORG_ID and VERCEL_PROJECT_ID set as job-level env vars (not step env) — required by Vercel CLI for all steps"

patterns-established:
  - "vercel pull --environment=production downloads project config and dashboard env vars before build"
  - "vercel build --prod produces .vercel/output/ for prebuilt upload"
  - "vercel deploy --prebuilt --prod uploads without rebuilding"

requirements-completed: [MIG-01, MIG-03, MIG-04, MIG-06]

duration: ~3min
completed: "2026-03-29"
---

# Phase 01.1 Plan 02: Vercel Deployment Workflow Summary

**GitHub Pages workflow replaced with Vercel CLI pull/build/deploy pattern; .vercel/ excluded from git; human setup steps required before CI can run successfully.**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-03-29
- **Completed:** 2026-03-29
- **Tasks:** 1 of 2 auto-tasks complete (Task 2 is human-action checkpoint)
- **Files modified:** 2

## Accomplishments

- Replaced GitHub Pages `deploy.yml` with Vercel Production Deployment workflow using `vercel pull / vercel build --prod / vercel deploy --prebuilt --prod` pattern
- Added `.vercel/` to `.gitignore` so `vercel link` output (containing orgId and projectId) is never committed
- No GitHub Pages permissions block (`pages: write`, `id-token: write`) in new workflow

## Task Commits

1. **Task 1: Add .vercel/ to .gitignore and replace deploy.yml with Vercel workflow** - `74989f3` (chore)

## Files Created/Modified

- `.github/workflows/deploy.yml` — Vercel Production Deployment workflow (replaces GitHub Pages workflow entirely)
- `.gitignore` — Added `.vercel/` entry

## Decisions Made

- Used `vercel pull / vercel build / vercel deploy --prebuilt` three-step pattern (not single `vercel deploy`) — this is the recommended GitHub Actions pattern; `vercel pull` downloads Vercel project config and any env vars set in dashboard before build, ensuring environment is consistent with Vercel dashboard configuration
- Belt-and-suspenders `env:` block on the Build step ensures Supabase PUBLIC_ vars are available during build even if not yet added to Vercel dashboard — prevents partial failures during initial setup
- `VERCEL_ORG_ID` and `VERCEL_PROJECT_ID` set at job `env:` level (not individual step) — required by Vercel CLI for authentication across all steps

## Deviations from Plan

None — plan executed exactly as written.

## User Setup Required

Task 2 is a `checkpoint:human-action` requiring the following before CI succeeds:

1. Create Vercel project and import the GitHub repo at vercel.com
2. Get `VERCEL_ORG_ID` and `VERCEL_PROJECT_ID` (via `vercel link` CLI or Vercel dashboard)
3. Create `VERCEL_TOKEN` at vercel.com → Account Settings → Tokens
4. Add `VERCEL_TOKEN`, `VERCEL_ORG_ID`, `VERCEL_PROJECT_ID` as GitHub repository secrets
5. Add `PUBLIC_SUPABASE_URL` and `PUBLIC_SUPABASE_ANON_KEY` as Vercel dashboard environment variables (Production)
6. Update Supabase Auth URL Configuration: Site URL → `https://www.theaiq.org`, add `https://www.theaiq.org/auth/callback` to Redirect URLs
7. Trigger a test deployment and confirm green in GitHub Actions
8. Disable GitHub Pages after Vercel is confirmed working (GitHub repo → Settings → Pages → Source → None)

## Next Phase Readiness

- CI workflow is wired and ready — will deploy to Vercel the moment GitHub Secrets are in place
- Supabase auth redirect must be updated in dashboard before magic link auth works on the new domain
- After Vercel is confirmed green, Phase 01.1 Plan 03 (DNS configuration for www.theaiq.org) can proceed

## Self-Check: PASSED

- `.github/workflows/deploy.yml` found and contains `vercel deploy --prebuilt --prod` — PASS
- `.github/workflows/deploy.yml` contains no "GitHub Pages" references — PASS
- `.gitignore` contains `.vercel/` entry — PASS
- Task 1 commit `74989f3` found in git log — PASS

---
*Phase: 01.1-platform-migration*
*Completed: 2026-03-29*
