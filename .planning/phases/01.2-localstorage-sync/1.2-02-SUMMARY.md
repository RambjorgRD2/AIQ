---
phase: "1.2"
plan: "02"
subsystem: "write-hooks"
tags: ["supabase", "dual-write", "localStorage", "sign-in-migration"]
requires: ["src/lib/db.ts", "src/lib/supabase.ts"]
provides: ["dual-write in all lesson pages", "assessment write", "sign-in migration in Layout.astro"]
affects: ["src/pages/courses/foundations/[lesson].astro", "src/pages/courses/workflows/[lesson].astro", "src/pages/courses/leadership/[lesson].astro", "src/pages/assessment.astro", "src/layouts/Layout.astro"]
tech-stack-added: []
tech-stack-patterns: ["fire-and-forget .catch(console.error)", "async event listener", "static imports in Astro script blocks"]
key-files-created: []
key-files-modified:
  - src/pages/courses/foundations/[lesson].astro
  - src/pages/courses/workflows/[lesson].astro
  - src/pages/courses/leadership/[lesson].astro
  - src/pages/assessment.astro
  - src/layouts/Layout.astro
decisions:
  - "Static imports used instead of dynamic import() — Vite/Astro does not resolve relative dynamic imports in script blocks at build time"
  - "assessment.astro uses async IIFE to avoid converting the outer IIFE to async"
  - "migrateLocalStorageToSupabase handles both old (r.tier.tier) and flat (r.tier) result shapes"
duration: "12m"
completed: "2026-04-04"
---

# Phase 1.2 Plan 02: Write Hooks + Sign-in Migration Summary

**One-liner:** Dual-write Supabase hooks in all three course lesson templates and assessment page, plus a SIGNED_IN migration handler in Layout.astro that batch-upserts all existing localStorage data on sign-in.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Lesson files dual-write (completion + reflection) | 3d2ec56 | foundations/[lesson].astro, workflows/[lesson].astro, leadership/[lesson].astro |
| 2 | Assessment write + Layout.astro sign-in migration | 3d2ec56 | assessment.astro, Layout.astro |

## Deviations from Plan

**1. [Rule 3 - Bug] Static imports instead of dynamic import()**
- **Found during:** Task 1 — first build attempt
- **Issue:** Vite cannot resolve relative `import('../../lib/db.ts')` dynamic imports inside Astro script blocks at build time. Error: "Could not resolve ../../lib/db.ts"
- **Fix:** Changed to static `import { upsertLessonProgress, upsertReflectionNote } from '../../../lib/db.ts'` at top of each script block
- **Commit:** 3d2ec56

**2. [Rule 1 - Bug] Wrong import path depth**
- **Found during:** Task 1 — second build attempt
- **Issue:** Used `../../lib/` but course pages live at `src/pages/courses/{course}/`, requiring `../../../lib/`
- **Fix:** Corrected all three lesson files to `../../../lib/supabase.ts` and `../../../lib/db.ts`
- **Commit:** 3d2ec56

## Self-Check: PASSED

- Commit `3d2ec56` exists: FOUND
- `npm run build` passes: CONFIRMED
- All five files contain Supabase dual-write code: CONFIRMED
- Layout.astro contains `SIGNED_IN` handler: CONFIRMED
